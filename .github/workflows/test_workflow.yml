name: Testing
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          echo "**** Hello *****"
          echo "**** ${{ ls }} *****"